name: Update Auth0 Branding and prompts
on:
  workflow_dispatch:
    inputs:
      use-emoji:
        type: boolean
        description: 'Want to deploy on Prod'
        required: true

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello World!"
